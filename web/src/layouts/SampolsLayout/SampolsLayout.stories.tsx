import type { Meta, StoryObj } from '@storybook/react'

import SampolLayout from './SampolsLayout'

const meta: Meta<typeof SampolLayout> = {
  component: SampolLayout,
}

export default meta

type Story = StoryObj<typeof SampolLayout>

export const Primary: Story = {}
