import type { Meta, StoryObj } from '@storybook/react'

import RefineLayout from './RefineLayout'

const meta: Meta<typeof RefineLayout> = {
  component: RefineLayout,
}

export default meta

type Story = StoryObj<typeof RefineLayout>

export const Primary: Story = {}
